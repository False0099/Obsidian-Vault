## 选题内容：
1. 2024 专业竞赛获奖相关主题（数据集（网络搜索）+模型+可视化）
2. google Scholar，图书馆外文库（Web of Science+arxiv）复现->新的改进提升->

## 基础知识
1. 了解涉及到我们的无线网络的对应的七层协议中的对应层和对应的内容。了解无线网络的各层结构：1. 物理层中的天线对应的类型，天线阵列，**蜂窝网络**。我们无线网络通信中的一些评估指标：SNR（信号的信息量），RSST（信号的强度）。
2. 链路层：信道的分用（解决冲突）复用（提高性能）。（**CSMA/CA** 协议）无线网络如何能够识别出我们的邻居，怎么控制我们的对应的拓扑结构。
3. 网络层：路由，路由算法中的漏洞与攻击（通过伪造对应的报文来截取我们的对应的数据），
4. 传输层：TCP 的对应的信息。**二进制退避算法的缺陷（效率不高**）。性能：带宽 (band width, 吞吐量，有效吞吐量)，延时，每一个报文的平均发送次数，Qos（服务质量，综合指标，评价算法）QoE（体验质量，综合指标，评价算法）。
5. 重要的数据：1. Beacon （信标报文），crosslayer optimization (跨层优化)
6. 无线协议的标准：IEEE, IETF（IEEE 802.11 协议，主要服务于我们的 WLAN，信道划分，将我们的信道按照频率进行划分，要求我们的各个划分之间不能有干扰）我们的协议的最大速率和我们的实际速率存在差错：1. 冗余信息 2. 控制报文

## HMWK
1. 找到我们的 Beacon 报文的特征，测量 Beacon 报文的间隔是多少。包含了什么信息（**报文格式**）。（获得我们的邻居节点）
2. 破解 WIFI 认证（WPA/WPA 2 为主），密码需要有强度，密码的内容需要包括字母，数字，特殊字符，可以使用字典，字典生成工具。需要提供生成字典的命令。
3. 区分我们的无线网络报文的内容和对应的类型。