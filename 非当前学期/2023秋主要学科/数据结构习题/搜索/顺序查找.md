下一步：[[非当前学期/2023秋主要学科/数据结构习题/搜索/二叉查找树|二叉查找树]]

## 折半查找判定树
第一步：找到我们的第一个元素的下表和我们的第二个下标的和除以 2 向下取整
（1+12）/2=6, 我们把 6 作为我们的根节点，
第二步: 我们找我们 19 的左子树，我们有 $\frac{1+5}{2}=3$,也就是我们的 3 号下表所在的结点作为我们的第一个左儿子。
第三步，我们找 3 号下表对应的左子树，也就是我们的 $\frac{1+2}{2}=1$ 我们把我们的 1 放到我们的左子树上
第四步，我们对于我们的 1 号节点的右子树，我们就有：2
![[Pasted image 20240102170144.png]]

![[Pasted image 20240104170251.png]]


## 定义
![[Pasted image 20231226161311.png]]
1. A (也就是我们常说的现行的查找方法)
2. **B**（注意：我们这里求得是**成功查找**，那么我们的次数就是对应的位置）![[Pasted image 20231212145801.png]]
![[Pasted image 20231226161258.png]]
5. D (如果不是顺序那么我们取位置也要花 $o(n)$,所以我们一定只能是顺序存储方式)
![[Pasted image 20231212144349.png]]
6. C (在极端数据的情况下会退化，比如我们的第一个元素)
7. B（判定树是这样的）
8. B（这个还得看我们的常数，一般来说，二叉排序树的常数会相对较大）
9. B（根据我们的定义进行即可，第一次，$mid=\frac{1+11}{2}=6$），第二次, $mid=\frac{(1+6)+11}{2}$.
10. **B**（最后我们 mid 直接指向了我们的结果，我们不记录这一次，第一次，比较我们的 ）
11. A（特殊值代入, 对 $n$ 个结点的判定树，设结点总数 $n=2^h-1$, 则 $h=\lceil\log_2(n+1)\rceil$。另解：特殊值代入法。直接将 $n=1$ 和 $n=2$ 的情况代入，仅有 A 满足要求。）
12. A, B 对于此类题，有两种做法：一种方法是，画出查找过程中构成的判定树，让最小的分支高度对应于最少的比较次数，让最大的分支高度对应于最多的比较次数，出现类似于长度为 15 的顺序表时，判定树刚好是一棵满树，此时最多比较次数与最少比较次数相等；另一种方法是，直接用公式求出最小的分支高度和最大分支高度，从前面的讲解不难看出最大分支高度为 $H=\lceil\log_2(n+1)\rceil=5$,这对应的就是最多比较次数，然后由于判定树不是一棵满树，所以至少应该是 4 (由判定树的各分支高度最多相差 1 得出)。注意，若是求查找成功或查找失败的平均查找长度，则需要画出判定树进行求解。此外，对长度为 n 的有序表，采用折半查找时，查找成功和查找失败的最多比较次数相同，均为「log $_2(n+1)\rceil。$




![[Pasted image 20231212144443.png]]
19. A（只能往中间走，不能忘两侧走）
20. B
![[Pasted image 20231212144455.png]]
21. A![[Pasted image 20231226163236.png]]

## 计算：
![[Pasted image 20231226161235.png]]
3. B（平均长度=总长度/总方案书=(n+1)/2）
4. A (因为进行顺序查找，我们就可以计算我们的 1/2 * 1+1/3 * 2+1/6 * 3=A)
![[Pasted image 20231212144425.png]]
13. A, D

假设有序表中元素为 A[0... 11], 不难画出对它进行折半查找的判定树如下图所示，圆圈是查找成功结点，方形是虚构的查找失败结点。从而可以求出查找成功的 ASL$= ( 1+ 2\times 2+ 3\times 4+ 4\times 5) /12=$ 37/12, 查找失败的 ASL $=(3\times3+4\times10)/13$。
![[Pasted image 20231226162632.png]]
 注意：对于本类题目，应先根据所给 $n$ 的值，画出如上图的折半查找判定树。另外，查找失败结点的 ASL 不是图中的方形结点，而是方形结点上一层的圆形结点。

14. B（与我们竞赛的分块不相同，这个要求我们的块间有序，让我们能够快速找到我们的结果）
15. A（最理想的就是开个根号，实在不行最好也要接近根号）
16. B 根据公式 ASL $=L_1+L_s=\frac{b+1}{2}+\frac{s+1}{2}=\frac{s^2+2s+n}{2s}$,其中 $b=n/s,s=123/3,n=123$, 代入不难 得出 ASL 为 23。故选 B。
17. C (最多就是最后一块，最后一个，就是我们的 $2\sqrt{ N }$), 然后我们就可以通过我们的 $\log block$ 来处理
18. B 折半查找不成功的最大长度就是我们的判定树的高度，

## 计算大题
（**易错**）
![[Pasted image 20231226163338.png]]
（1）平均查找长度不同。因为有序顺序表查找到其关键字值比要查找值大的元素时就停止查找，并报告失败信息，不必查找到表尾；而无序顺序表必须查找到表尾才能确定查找失败。
（2）平均查找长度相同。两者查找到表中元素的关键字值等于给定值时就停止查找。
（3）平均查找长度不同。有序顺序表中关键字相等的元素相继排列在一起，只要查找到第一个就可以连续查找到其他关键字相同的元素。而无序顺序表必须查找全部表中的元素才能找出相同关键字的元素，因此所需的时间不同。

（判定树的构造方法）

![[Pasted image 20231226163557.png]]
![[Pasted image 20231226163625.png]]
（判定树的构造）
![[Pasted image 20231226163958.png]]
![[Pasted image 20231226164030.png]]

![[Pasted image 20231226164243.png]]
![[Pasted image 20231226164254.png]]